before-command: |
  #!/bin/bash
  set -e

  npm install
preview-commands:
  - id: vite-dev-server
    title: Vite Dev Server
    description: Starts the Vite development server for the Zombie Survival game on
      port 3000
    command: |
      #!/bin/bash
      set -e

      npm run dev
preview-port: 3000
